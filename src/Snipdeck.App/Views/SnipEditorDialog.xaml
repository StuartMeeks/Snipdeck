<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Snipdeck.App.Views.SnipEditorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Snipdeck.Core.ViewModels"
    xmlns:models="using:Snipdeck.Core.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Edit snip"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <ScrollViewer MinWidth="520" MaxHeight="560" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="12">
            <TextBox Header="Title"
                     Text="{x:Bind ViewModel.Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBox Header="Command template"
                     Text="{x:Bind ViewModel.CommandTemplate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     FontFamily="Cascadia Mono, Consolas, Courier New"
                     AcceptsReturn="True"
                     TextWrapping="Wrap" />

            <TextBox Header="Description (Markdown)"
                     PlaceholderText="Supports Markdown — **bold**, `code`, lists, links"
                     Text="{x:Bind ViewModel.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Height="80" />

            <TextBox Header="Tags (comma-separated)"
                     Text="{x:Bind ViewModel.TagsText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="Parameters"
                           Style="{ThemeResource BodyStrongTextBlockStyle}"
                           VerticalAlignment="Center" />
                <Button Grid.Column="1"
                        Content="Add parameter"
                        Click="OnAddParameterClicked" />
            </Grid>

            <ItemsControl ItemsSource="{x:Bind ViewModel.Parameters}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Spacing="8" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:ParameterEditorRowViewModel">
                        <Border Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                CornerRadius="6"
                                Padding="12">
                            <StackPanel Spacing="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0"
                                             Header="Name"
                                             Text="{x:Bind Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Grid.Column="1"
                                            Margin="8,24,0,0"
                                            Click="OnRemoveParameterClicked"
                                            Tag="{x:Bind}"
                                            ToolTipService.ToolTip="Remove parameter">
                                        <FontIcon Glyph="&#xE74D;" FontSize="14" />
                                    </Button>
                                </Grid>
                                <ComboBox Header="Type"
                                          SelectedIndex="{x:Bind TypeIndex, Mode=TwoWay}"
                                          HorizontalAlignment="Stretch">
                                    <x:String>Text</x:String>
                                    <x:String>Choice</x:String>
                                </ComboBox>
                                <TextBox Header="Default"
                                         Text="{x:Bind Default, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Header="Options (comma-separated, Choice only)"
                                         Text="{x:Bind OptionsText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{x:Bind IsChoice, Mode=OneWay}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
