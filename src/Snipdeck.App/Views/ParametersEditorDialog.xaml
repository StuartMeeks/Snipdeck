<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="Snipdeck.App.Views.ParametersEditorDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Snipdeck.Core.ViewModels"
    xmlns:converters="using:Snipdeck.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="EditorDialog"
    Title="{x:Bind ViewModel.Title}"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <ContentDialog.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <x:Double x:Key="ContentDialogMaxWidth">760</x:Double>
    </ContentDialog.Resources>

    <ScrollViewer MinWidth="600" MaxHeight="560" Padding="0,0,12,0" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="12">
            <Button Content="Add parameter"
                    HorizontalAlignment="Left"
                    Command="{x:Bind ViewModel.AddParameterCommand}" />

            <TextBlock Text="No parameters yet. Add one above."
                       Style="{ThemeResource BodyTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay, Converter={StaticResource BoolToVisibility}}" />

            <ItemsControl ItemsSource="{x:Bind ViewModel.Parameters}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Spacing="8" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="vm:ParameterEditorRowViewModel">
                        <Border Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                CornerRadius="6"
                                Padding="12">
                            <StackPanel Spacing="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBox Grid.Column="0"
                                             Header="Name"
                                             Text="{x:Bind Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    <Button Grid.Column="1"
                                            Margin="8,24,0,0"
                                            Command="{Binding ElementName=EditorDialog, Path=ViewModel.RemoveParameterCommand}"
                                            CommandParameter="{x:Bind}"
                                            ToolTipService.ToolTip="Remove parameter">
                                        <FontIcon Glyph="&#xE74D;" FontSize="14" />
                                    </Button>
                                </Grid>
                                <ComboBox Header="Type"
                                          SelectedIndex="{x:Bind TypeIndex, Mode=TwoWay}"
                                          HorizontalAlignment="Stretch">
                                    <x:String>Text</x:String>
                                    <x:String>Choice</x:String>
                                </ComboBox>
                                <TextBox Header="Default"
                                         Text="{x:Bind Default, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                <TextBox Header="Options (comma-separated, Choice only)"
                                         Text="{x:Bind OptionsText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         IsEnabled="{x:Bind IsChoice, Mode=OneWay}" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</ContentDialog>
